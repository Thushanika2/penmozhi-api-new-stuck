MONGODB_URI=mongodb://localhost:27017/penmozhi
NODE_ENV=development
PORT=5000
PENMOZHI_DEBUG=false

# Legacy MySQL source connection, used only by scripts/migrate-mysql-to-mongodb.js.
MYSQL_URL=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=3306
DB_NAME=penmozhi_db

# Required for persistent production sessions. Generate a unique random value
# of at least 32 characters; never copy a shared example secret.
JWT_SECRET_KEY=
JWT_ACCESS_TOKEN_EXPIRES_MINUTES=1440
JWT_REFRESH_TOKEN_EXPIRES_DAYS=30
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CLIENT_APP_URL=http://localhost:3000

# Optional SMTP for password reset emails (if unset, DEBUG mode returns token in API response)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_USE_TLS=true

# Brevo transactional email (required for cycle-sharing invitations).
# BREVO_FROM_EMAIL must be a sender/domain verified in your Brevo account.
BREVO_API_KEY=
BREVO_FROM_EMAIL=
BREVO_FROM_NAME=Penmozhi

ADMIN_NAME=Admin User
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me

# LLM (AI assistant) — provider: auto | gemini | anthropic
AI_PROVIDER=auto
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-lite-latest
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-haiku-20241022

# Web Push (VAPID keys)
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
VAPID_CLAIMS_EMAIL=mailto:admin@penmozhi.com

# Scheduled push notifications. Enable this on exactly one Railway API replica;
# leave it false on every other replica to prevent duplicate deliveries.
ENABLE_SCHEDULER=false
SCHEDULER_INTERVAL_MINUTES=1

# Cloudinary (education video hosting — free tier)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
# Optional legacy multipart request limit in bytes (default 210MB, including
# multipart overhead around the 200MB video-file limit)
# MAX_CONTENT_LENGTH=220200960

# Wearable OAuth stubs — TODO: configure per provider
OURA_CLIENT_ID=
OURA_CLIENT_SECRET=
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=
FITBIT_CLIENT_ID=
FITBIT_CLIENT_SECRET=
WITHINGS_CLIENT_ID=
WITHINGS_CLIENT_SECRET=

# Payment stubs — TODO: real payment integration
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

